version: '3'

networks:
  hostnet:
    external:
      name: host

services:
  vueui:
    container_name: vueui
    image: yuanjunjie/foundation:dev
    restart: always
    ports:
      - '8080:80'
    networks:
      - hostnet
